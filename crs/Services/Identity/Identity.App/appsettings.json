{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "JwtSettings": {
    "SecretKey": "This is the secret key used to sign the JWT tokens. It can be any string.",
    "Issuer": "The issuer of the JWT tokens. Usually your application URL.",
    "Audience": "The audience of the JWT tokens. Usually your application URL."
  },
  "DefaultConnection": "Host=localhost;Port=5432;Database=identityDb;Username=postgres;Password=postgres1Ipw;",
}
